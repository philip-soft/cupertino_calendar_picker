name: cupertino_calendar
description: The cupertino calendar package.
version: 1.0.0
repository: https://github.com/philip-soft/cupertino_calendar

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2
  flutter_test:
    sdk: flutter
